version: '3'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/usr/src/app
    ports:
      - '5000:5000'
    env_file:
      - .env
    tty: true
    depends_on:
      - mongo
    links:
      - mongo
    networks:
      - mongo-compose-network

  mongo:
    image: mongo
    environment:
      - MONGO_INITDB_DATABASE=database
    ports:
      - '27018:27017'
    volumes:
      - mongodata:/data/db
    networks:
      - mongo-compose-network

networks:
  mongo-compose-network:
    driver: bridge
volumes:
  mongodata:
